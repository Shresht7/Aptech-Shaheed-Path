export interface CourseDetails {
    id: string;
    cover: string;
    title: string;
    description: string;
    duration: string;
    fee: string;
    logo: string;
    priority: number
}

export interface JobDetails {
    title: string;
    description: string;
}
